namespace SufiChain.SufiBlazor.Components.Conversation;

/// <summary>
/// Horizontal alignment of a conversation message bubble.
/// </summary>
public enum SbConversationAlignment
{
    /// <summary>Message appears on the start side (typically peer / incoming).</summary>
    Start,

    /// <summary>Message appears on the end side (typically self / outgoing).</summary>
    End
}
